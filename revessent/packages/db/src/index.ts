/**
 * @revessent/db — PostgreSQL persistence (Phase 3).
 * Schema = Architecture v1 §5.2. Migrations in ./drizzle (drizzle-kit).
 * Client + tenancy primitives in ./client.
 */
export * from "./schema.js";
export * from "./client.js";
